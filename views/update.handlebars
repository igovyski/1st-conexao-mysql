<h2>Editar livro</h2>

<form action="/update/:id/save" method="POST">
    <div>
        <label for="title">Titulo</label>
        <input type="text" name="title" placeholder="Digite o titulo" id="title" value="{{book.title}}">
    </div>
    <div>
        <label for="pageqty">Numero de Pg</label>
        <input type="text" name="pageqty" placeholder="Digite o numero de pgs" id="pageqty" value="{{book.pageqty}}">
    </div>
        <div>
        <label for="descricao">Descrição</label>
        <input type="text" name="descricao" placeholder="Digite a descrição do livro" id="descricao" value="{{book.descricao}}">
    </div>

    <button type="submit">Salvar</button>
</form>